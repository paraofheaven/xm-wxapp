<block wx:if="{{$taroCompReady}}">
    <view>
        <view class="demo-otp-modal">
            <otp-modal compid="{{$compid__16}}"></otp-modal>
        </view>
        <at-message></at-message>
    </view>
</block>