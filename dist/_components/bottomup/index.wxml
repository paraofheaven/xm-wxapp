<block wx:if="{{$taroCompReady}}">
    <block wx:if="{{!visible}}">
        <view></view>
    </block>
    <block wx:else="{{true}}">
        <view class="{{bottomClassName}}" catchtouchmove="handleTouchMove">
            <block wx:if="{{!hideMask}}">
                <view class="v-bottomup-mask" bindtap="closeByDocument"></view>
            </block>
            <view class="v-bottomup-wrap">
                <view class="v-bottomup-content">
                    <view class="v-class-title v-bottomup-header">
                        <block wx:if="{{title}}"><text class="v-bottomup-title">{{title}}</text>
                        </block>
                        <my-icon v-class="v-icon-class" compid="{{$compid__191}}"></my-icon>
                    </view>
                    <slot></slot>
                </view>
            </view>
        </view>
    </block>
</block>